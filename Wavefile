[project]
name = "gitView"
version = "0.1.0"
owner = "bouajilaProg"
category = "personal"
tags = ["dev-tools","git"]

[flow]
  dev = {
     cmd= "./tools/tailwindcss -c tailwind.config.js -i src/viewer/style/tailwind.css -o src/viewer/style/output.css && go build -o gitView ./src",
     on_success= "mv ./gitView ../test/gitView && cd ../test && ./gitView",
     watch=["*.go","assets/*","src/**/*","tailwind.config.js","src/viewer/style/tailwind.css"]
   }
  build = {
     cmd= "./tools/tailwindcss -c tailwind.config.js -i src/viewer/style/tailwind.css -o src/viewer/style/output.css --minify && go build -o gitView ./src"
   }
  test = {
     cmd= "go test ./src"
   }
  setup_linux = {
     cmd= "sh tools/setup-tailwind-linux.sh"
   }
  setup_windows = {
     cmd= "powershell -ExecutionPolicy Bypass -File tools/setup-tailwind-windows.ps1"
   }
  ci = {
     cmd= "./tools/tailwindcss -c tailwind.config.js -i src/viewer/style/tailwind.css -o src/viewer/style/output.css --minify && go test ./src && go build -o gitView ./src"
   }
